/** @type {import('next').NextConfig} */
const nextConfig = {
    typescript: {
      ignoreBuildErrors: true, // This will prevent the build from failing due to TypeScript errors
    },
  };
  
  export default nextConfig;